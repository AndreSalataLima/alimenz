<%= render 'admin/shared/navbar', titulo: "Produto: #{@product.generic_name}" %>

<div class="container mx-auto py-6 px-4">
  <div class="max-w-xl mx-auto bg-white shadow-md rounded-xl p-6">
    <h2 class="text-2xl font-bold text-gray-800 mb-4">Detalhes do Produto</h2>

    <ul class="space-y-2 text-gray-700">
      <li><strong>Nome Genérico:</strong> <%= @product.generic_name %></li>
      <li><strong>Unidades:</strong> <%= @product.unit_options&.join(", ") %></li>
    </ul>

    <div class="mt-6 flex space-x-4">
      <%= link_to "Editar", edit_admin_product_path(@product), class: "px-4 py-2 bg-green-600 text-white rounded-lg shadow hover:bg-green-700" %>
      <%= link_to "Voltar", admin_products_path, class: "px-4 py-2 bg-gray-400 text-white rounded-lg shadow hover:bg-gray-500" %>
    </div>
  </div>
</div>
