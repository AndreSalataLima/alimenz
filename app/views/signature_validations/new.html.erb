<div class="min-h-screen bg-gray-50 flex items-center justify-center p-6">
  <div class="bg-white w-full max-w-lg rounded-2xl shadow p-8">
    <h1 class="text-xl font-semibold text-center mb-6">Validar documento</h1>

    <% if flash[:alert].present? %>
      <div class="mb-4 rounded-lg bg-red-50 text-red-700 p-3"><%= flash[:alert] %></div>
    <% end %>

    <%= form_with url: signature_validations_path, method: :post, local: true do |f| %>
      <label class="block text-sm text-gray-700 mb-1">Código de rastreio</label>
      <%= text_field_tag :signature_tracking_id, params[:signature_tracking_id],
            class: "w-full rounded-lg border-gray-300 focus:ring focus:ring-indigo-200 focus:border-indigo-500",
            placeholder: "Ex.: ABCD-1234-XYZ" %>

      <button type="submit"
              class="mt-4 w-full py-2 px-4 bg-[#615f4a] text-white rounded-lg hover:bg-[#4b493f]">
        Validar
      </button>
    <% end %>
  </div>
</div>
